apiVersion: v1
kind: Pod
metadata:
  name: 你的项目的名字（不要删除空格）
spec:
  containers:
  - name: 你的项目的名字（不要删除空格）
    image: 192.168.12.109/smart/ubuntu:sc1
    # Just spin & wait forever
    command: [ "/bin/bash", "-c", "--" ]
    args: [ "while true; do sleep 30; done;" ]